# services/api

Backend API chính thức (`api.tms.name.vn`).

## Chạy local

```bash
npm install
npm run dev
```

Mặc định chạy tại `http://localhost:5000`.

## Ghi chú

- Đây là vị trí source API đang dùng hiện tại.
- Xem tài liệu API đơn hàng tại `ORDERS_API.md`.
