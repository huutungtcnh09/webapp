# apps/pos

App POS chính thức (`pos.tms.name.vn`).

## Chạy local

```bash
npm install
npm run dev:pos
```

Mặc định chạy tại `http://localhost:5174`.

## Ghi chú

- Đây là vị trí source POS đang dùng hiện tại.
